<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="wiki.common_cat.tokenService.mapper.TokenMapper">
    <select id="getUser" resultType="wiki.common_cat.logService.entities.User">
        select * from user where id=#{id}
    </select>
</mapper>