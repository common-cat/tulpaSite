<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="wiki.common_cat.signService.mapper.SignMapper">
<insert id="newUser">
    insert into users (id,date,salt,pwdhash,lastLoginDate,status,loginTimes) values (#{id},#{date},#{salt},#{pwdhash},#{lastLoginDate},#{status},0)
</insert>
</mapper>